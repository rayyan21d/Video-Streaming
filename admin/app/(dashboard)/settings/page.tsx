"use client";
import { MovieGrid } from "@/components/MovieGrid";

export default function Profile() {
  return (
    <div className="flex flex-col min-h-screen bg-black text-white">
      <main className="flex-grow">Settings</main>
    </div>
  );
}
